import { Container, Typography } from '@mui/material';

export default function RiderDashboard() {
  return (
    <Container>
      <Typography variant="h4">Rider Dashboard</Typography>
      <Typography>View your delivery stats</Typography>
    </Container>
  );
}
