import { Container, Typography } from '@mui/material';

export default function WarehouseOrderDetails() {
  return (
    <Container>
      <Typography variant="h4">Order Details</Typography>
      <Typography>View and manage order details</Typography>
    </Container>
  );
}
