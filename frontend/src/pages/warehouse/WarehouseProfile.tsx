import { Container, Typography } from '@mui/material';

export default function WarehouseProfile() {
  return (
    <Container>
      <Typography variant="h4">Warehouse Profile</Typography>
      <Typography>Manage warehouse settings and information</Typography>
    </Container>
  );
}
